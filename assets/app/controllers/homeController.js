app.controller("HomeController", [
    "$scope",
    "$location",
    "$log",
    "uiGmapGoogleMapApi",
    function ($scope, $location, $log, uiGmapGoogleMapApi) {
        'use strict';

        $log.debug("Home Controller Initialized");
    }]);